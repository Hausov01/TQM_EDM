<Window x:Class="Test_WPF.Main_menu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test_WPF"
        mc:Ignorable="d"
        Title="Window1" Height="800" Width="1200">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Меню -->
            <ColumnDefinition Width="0.2*"/>
            <!-- Область вкладок -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Меню -->
        <StackPanel>
            <TextBlock  Text="Накладная" FontSize="14" MouseLeftButtonDown="NewTab"/>
            <TextBlock  Text="Отчёт о проверке партии" FontSize="14" MouseLeftButtonDown="NewTab"/>
            <TextBlock  Text="Акт о браке продукции" FontSize="14" MouseLeftButtonDown="NewTab"/>
            <TextBlock  Text="Итоговый отчёт" FontSize="14" MouseLeftButtonDown="NewTab"/>
        </StackPanel>

        <!-- Контейнер вкладок и дополнительная строка -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.1*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Content="Создать" HorizontalAlignment="Center" Width="60" Margin="10 10" FontSize="15" Click="CreateDoc"/>
                <Button Content="Удалить" HorizontalAlignment="Center" Width="60" Margin="10 10" FontSize="15" Click="DeleteDoc"/>
            </StackPanel>

                    <!-- контейнер вкладок -->
            <TabControl x:Name="TabsContainer" Grid.Row="1">
            </TabControl>
        </Grid>
    </Grid>
</Window>